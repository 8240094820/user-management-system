import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2",
    },
    secondary: {
      main: "#1565c0",
    },
    background: {
      default: "#f5f5f5",
    },
  },
});

export default theme;